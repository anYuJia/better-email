# Better Email 验证记录

## 当前验证时间

- 日期：2026-07-11
- 阶段：本地邮箱客户端原型；项目已统一命名为 `better-email` / `Better Email`，并加入旧版 localStorage、SQLite 数据库、Keychain 服务名和基准环境变量的兼容迁移；完成 QQ/Apple/Foxmail 参考下的平面三栏 UI 优化、设置中心独立分页与统一控件语言、统一右键菜单、文件夹全部标为已读/清空废纸篓、联系人右键编辑/删除、邮件拖入侧栏文件夹、常用邮箱固定与持久化、侧栏内约束的更多邮箱列表、安全移除邮箱账号、唯一默认发件账号、可配置撤销发送延迟、倒计时撤回与到期自动 SMTP 调度、SMTP 成功后的 IMAP 已发送目录留档、IMAP 新邮件/历史邮件双向分页、设置页手动历史回填和发送/回写四步验收，并完成网易 163 的脱敏 TLS 与授权码认证验证；继续保留多账号、Keychain、MIME 安全预览、OAuth2/XOAUTH2、SMTP multipart、后台任务、通知、稍后处理、附件分段下载和远端状态回写入口

## 已验证命令

```bash
npm run build
npm run bench
npm run bench:release
npm run bench:app
npm run bench:sync
npm test
npm run test:ui
BETTER_EMAIL_UI_CAPTURE_DIR=/tmp/better-email-ui-captures npm run test:ui
cargo fmt --manifest-path src-tauri/Cargo.toml
cargo test --manifest-path src-tauri/Cargo.toml
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
```

## 验证结果

- 前端生产构建通过：Vite/TypeScript 构建成功。
- 主界面视觉回归通过：在 1069×713 内容区确认三栏无横向裁切、40 封邮件正常加载、搜索条件菜单与阅读“更多”菜单不越界；在 1500×900 确认标题和操作栏并排、正文与快速回复完整利用宽屏空间。
- 侧栏视觉回归通过：品牌方块已由旧 `S` 修正为 `B`；常用邮箱增加一项后仍保持紧凑层级；展开“更多邮箱”时列表宽 244px、完整包含于 268px 侧栏内，不再被通用弹层规则拉伸到整窗宽度。
- 轻量基准入口通过：`npm run bench` 可输出前端构建产物、最大前端资源、Tauri debug/release 二进制、bundle 产物、样本 SQLite、当前进程内存采样和未覆盖缺口；`npm run bench:release` 使用 `tauri build --no-bundle` 生成 release 二进制后复测体积；`npm run bench:app` 可启动 release App、等待采样窗口并记录空闲 RSS 后退出。
- Release 体积样本：`npm run bench:release` 已生成 `/src-tauri/target/release/better-email`，当前二进制约 17.72 MB；真实挂载后的前端 dist 约 237.5 KB。Cargo release 目录是构建缓存，不作为用户可见 App 体积。
- Release App 启动/同步峰值/空闲内存样本：`BETTER_EMAIL_BENCH_SAMPLE_MS=8000 npm run bench:sync` 已成功启动 `/src-tauri/target/release/better-email`，采样窗口 8 秒，前端 ready 标记约 769.76ms，同步演练完成标记约 769.81ms，采样窗口 peak RSS 约 83.72 MB，窗口末尾 RSS 约 83.23 MB；此前破损 PNG 图标导致的 `invalid icon` 启动崩溃已通过重建标准 Tauri 多尺寸图标资源修复。
- 前端单元测试通过：51 个 Vitest 测试覆盖日期/附件大小格式化、回复/转发主题前缀、引用正文生成与 HTML 正文安全摘要回退、标准回复线程头生成与去重、转发附件筛选、未下载提示和附件元数据延迟边界、同步状态摘要、新邮件通知文案、多账号与会话静音通知路由、静音过滤后的实际提醒数量、服务商矩阵/预设一致性、网易 163/QQ/OAuth2/自定义邮箱凭据安全文案、远程图片信任输入生成、“已发送 · 留档待重试”状态和 SMTP 成功后不可撤回语义、后台协调器对最早 SMTP/远端留档重试项的选择和不重复 SMTP 的结果文案、账号连接控制器的已知/空服务商键归一化、默认兼容性记录和 IMAP/SMTP 部分认证结果映射、网易凭据拒绝/单协议部分成功/双协议认证成功三类四步连接诊断模型、只读服务商验收的全链路成功、IMAP 网络失败短路、SMTP 单独认证失败继续收信验证和 IMAP 单独认证失败停止四条分支，以及写入验收草稿的稳定时间编号、自发自收、无自动附件/定时发送、敏感信息警告、SMTP/Sent/收件/附件/回写状态推导、outbox 不误判为收件副本和账号级回写步骤持久化。
- 自动化 UI 回归通过：`npm run test:ui` 的 90 个核心断言使用 Chrome headless + mock Tauri 数据验证 Better Email 品牌标识、旧 `swiftmail.*` 设置键迁移、三栏布局持久化/重置、现代账号范围弹出菜单/独立账号切换、右键设置默认发件账号及默认标识、统一邮箱写信使用默认账号、QQ 预设新增账号、自动切换、默认系统文件夹和默认发件身份、账号移除完整邮箱确认、删除后回到已配置默认账号且账号菜单清理、更多邮箱列表边界、常用邮箱固定/刷新持久化/取消固定、文件夹右键全部标为已读/远端批量清空废纸篓、联系人右键编辑/删除入口、联系人 vCard 导入/导出、头像邮件列表、单封星标远端 `\\Flagged` 回写提示、邮件右键复制发件人邮箱/邮件主题、图标式高级搜索菜单、统一右键菜单及键盘子菜单、邮件拖入文件夹与撤销、全局 `Cmd/Ctrl + Z` 邮件操作撤销、`Cmd/Ctrl + A` 全选当前列表、键盘批量操作和 `Esc` 取消选择、直接删除、远端恢复到收件箱与永久 expunge、命令面板、线程阅读、会话右键静音与阅读页取消静音、保存搜索、联系人、写信自动保存/模板/附件/富文本/身份/签名、草稿保存并同步远端 Drafts、快速回复传递标准 `In-Reply-To` / `References`、设置标题栏固定服务器测试/保存主操作、12 个设置分类独立分页且一次只渲染当前页、900px 以下分组分类选择器、普通设置入口稳定回到账号首页、设置页标题与分页不再受到旧全局 header/footer 样式污染、账号创建默认收起但可直接展开、设置期间底层状态条与撤销提示不再遮挡弹窗、服务商感知安全凭据输入、显示/隐藏、快速清空、保存并验证、Keychain 凭据登录验证与 IMAP/SMTP 分项结果、服务商感知四步诊断、技术详情默认折叠、一键只读验收的服务器/登录/文件夹/邮件头四阶段成功、同步页写入验收生成默认发给当前账号且无附件/定时发送的唯一编号草稿、最近编号按账号持久化、五阶段状态刷新和回写四步门控、服务商真实验证状态自动回填、OAuth2 PKCE 启动/回调授权码记录/token 交换/token 刷新、第二账号服务器测试/登录验证/IMAP 文件夹发现/同步演练作用域、远端自定义目录一键创建同名本地文件夹并映射、手动历史回填一页及最低 UID 状态更新、手动同步一次扫描多个已映射文件夹、已映射自定义目录作为真实远端移动目标、撤销发送设置持久化、倒计时内撤回到草稿箱、到期自动进入已发送、批量动作、自定义文件夹、垃圾邮件、发件箱、设置、本地 EML 导入、本地备份、规则、原始 MIME 安全预览、稍后处理、撤销、快速回复、EML 导出、标签、顺序下载全部、下载失败断点续传、转发携带已下载原附件、阻止发件人和远程图片信任。设置视觉回归可通过 `BETTER_EMAIL_UI_CAPTURE_DIR` 输出桌面与窄屏 PNG。
- Rust 单元测试通过：110 项测试覆盖旧 `swiftmail.sqlite3` 数据库及 WAL 边车文件迁移、凭据验证报告成功/部分失败状态、消息/线程最新与最早排序、非法排序值安全回退、按账号与线程键持久化的会话静音及跨账号隔离、默认发件账号唯一性/切换/删除接管、账号删除约束和关联数据级联清理、本地 EML 导入落库、安全正文、联系人和附件文件、vCard 折行/首选邮箱/别名/VIP 解析与导出往返、按主邮箱导入创建/合并、OAuth2、IMAP/SMTP XOAUTH2、真实与虚拟文件夹批量已读、远端 UID 集合去重、首次最近页/增量页/历史页 UID 选择、无游标或 UIDVALIDITY 重置时的历史同步引导、历史最低 UID 游标推进与完成状态、邮件头远端星标导入、最近 FLAGS 快照更新已读/星标并移除服务器已删除邮件、UIDVALIDITY 变化时清除旧 UID 数据、`COPYUID`/`APPENDUID` 目标 UID 解析、IMAP 参数转义与换行拒绝、移动或留档后 UID 0 记录按 Message-ID 原地重绑定、稳定出站 Message-ID、EML/IMAP `In-Reply-To` 与 `References` 解析持久化、草稿线程头恢复和 SMTP 标准线程头渲染、草稿出站 MIME 重建与远端 Drafts mailbox/UID/Message-ID 持久化、SMTP 成功后的 `sent_remote_pending` 状态、五分钟远端留档重试窗口、远端留档成功后的 Sent mailbox/UID 绑定、按验证编号跨 outbox/Sent/Inbox 查询、废纸篓删除前远端引用快照、核心目录和已绑定自定义目录筛选、自定义远端目录拒绝误导入收件箱、自定义目录绑定/目标文件夹导入/解除绑定保护、已映射自定义文件夹远端移动目标解析与解除映射保护、无日志批量邮件头导入、数据层、协议、SMTP、IMAP 探测/映射/同步、后台任务、本地备份、稍后处理、发件箱重试、远程图片信任、联系人、规则、附件下载/大小保护、富文本出站清洗、SMTP multipart、HTML sanitizer、发件箱撤回和凭据边界。
- 网易 163 真实账号历史基础样本通过：2026-07-10 曾对 `imap.163.com:993` 与 `smtp.163.com:465` 完成 TLS 1.3 握手、证书链校验和客户端授权码认证；未保存账号、授权码或完整凭据到仓库/文档/日志，且未执行真实邮件发送。本结果只证明该次历史样本的基础连接与认证，不代表当前授权码仍然有效；附件、正文同步、状态回写、真实发送和压力样本仍需单独验证。
- 网易 163 授权码复测记录：2026-07-10 使用后续新提供的授权码再次验证时，IMAP 返回登录密码错误、SMTP 返回 `535 authentication failed`，完整邮箱与邮箱名前缀两种用户名格式均被拒绝；TLS 和服务端可达性正常，未发送邮件且凭据未落盘。该结果说明授权码样本需要重新生成，不能把历史通过样本等同于所有后续授权码持续有效。
- 只读服务商验收工具通过：`npm run probe:provider -- --list` 会以 SQLite 只读模式列出脱敏账号；`npm run probe:provider -- --account-id <id>` 从 Keychain 读取凭据后依次执行 IMAP 登录、SMTP 认证 + `NOOP`、文件夹发现和收件箱邮件头抓取，JSON 不包含完整邮箱、密码、Token、主题、发件人或文件夹名称。本机当前数据库只有示例账号且没有凭据，已验证其输出脱敏错误报告、跳过网络阶段并返回非零退出码；真实服务商结果仍必须在应用内配置账号与 Keychain 后采集。
- Clippy 通过：`cargo clippy --all-targets -- -D warnings` 无警告。

- 会话与搜索范围回归已覆盖：后端和 Hook 测试验证邮件/线程请求在当前文件夹、当前账号全部文件夹、全部账号全部文件夹三档范围中使用一致的账号与文件夹约束；会话静音按账号与线程键持久化，同一线程键在不同账号间保持隔离。Chrome headless UI 冒烟验证范围切换、跨文件夹命中、跨账号命中、清空恢复当前文件夹、搜索后线程结果同步收窄，以及右键静音与阅读页取消静音。本轮 `npm run test:ui` 共通过 90 项核心断言。
- 前端加载层已拆分：`useMailboxData` 集中管理邮件/会话共享作用域、排序、分页、刷新竞态、空状态和未读回退，`buildMailboxRequests` 覆盖三档搜索范围和共享排序参数；连接诊断模型与界面已独立为 `connectionDiagnostics.ts` 和 `ConnectionDiagnosticsPanel.tsx`，服务商凭据文案与安全输入分别位于 `providerCredentialGuidance.ts` 和 `CredentialSecuritySettings.tsx`，只读服务商验收编排位于 `providerValidation.ts`，写入验收草稿与五阶段状态机位于 `providerWriteValidation.ts`，写入验收界面与回写步骤分别位于 `ProviderWriteValidationSettings.tsx`、`ProviderWritebackValidationPanel.tsx`，账号级编号、跨文件夹刷新、定位和回写步骤持久化编排位于 `useProviderWriteValidation.ts`；账号、服务商、认证、发送、通知、隐私和身份设置页已拆为独立页面组件，`AccountConnectionSettings` 与 `ExperienceSettings` 仅负责轻量路由。写信窗口、设置中心、命令面板和快捷键帮助均已转为按需异步 chunk，完整 UI 回归覆盖首次打开路径。当前验证总计为 51 项 Vitest、110 项 Rust 测试和 90 项 Chrome headless UI 断言。

## 已覆盖功能

| 功能域 | 当前状态 | 说明 |
| --- | --- | --- |
| 跨平台桌面壳 | 已实现 | Tauri + 系统 WebView，避免 Electron 常驻 Chromium；release 启动图标资源已重建为 PNG/ICNS/ICO 多尺寸资产 |
| 本地数据库 | 已实现 | SQLite + WAL，含账号、文件夹、邮件、标签、附件元数据，并支持不含 Keychain 凭据的本地 JSON 备份/恢复 |
| 三栏邮箱 UI | 已实现 | 文件夹栏、头像邮件列表、线程列表、阅读面板、撰写窗口、设置窗口，并支持侧边栏/邮件列表拖拽宽度、localStorage 持久化和恢复默认；低频文件夹可固定到常用邮箱并在刷新后保留，更多邮箱始终限制在侧栏内滚动；采用平面分栏、中性选中态和紧凑图标栏，高频回复/转发/归档/已读/删除直接可见，高级搜索、低频整理和安全动作按需展开；设置窗口由 `SettingsFrame` 统一管理 12 个独立分页、上一项/下一项、纯图标无障碍关闭和顶部保存操作，一次只渲染当前页，服务器测试只在连接相关页面出现；900px 以下分类侧栏替换为带分组的单一分类选择器，680px 以下标题栏操作收紧，同时保持设置、快捷键和命令三个紧凑图标入口可达；账号、服务商、认证、发送、通知、隐私和身份已拆为独立页面组件，核心设置进入页面即可操作，不再套冗余折叠；下拉框、开关、右键菜单、紧凑菜单、弹窗和右下状态浮层已统一最终视觉层，并确保 `ui-2026.css` 在组件样式后加载；认证页四个操作支持自动换行，服务器连接、系统凭据、IMAP、SMTP 四步诊断在宽屏使用紧凑双列、窄屏变为单列，技术详情默认折叠；一键只读验收使用同一区域内的紧凑四阶段条，桌面保持单行，`720×780` 窄屏变为两列；同步页的发送与回写验收首屏可见，五阶段卡片在桌面使用三列、`720×780` 使用单列，回写四步按钮按前置状态逐步解锁，操作按钮在窄屏使用两列加全宽主按钮，弹窗、诊断和写入验收区均无横向溢出；`Sidebar` 的账号切换和文件夹导航分别拆分到 `AccountSwitcher`、`SidebarFolderNavigation`，账号危险区、联系人管理、OAuth2 流程、写入验收和回写引导也已拆为独立组件/Hook；命令条目与过滤迁移到 `useCommandPaletteItems`，全局快捷键迁移到 `useAppShortcuts`，后台任务/定时同步/发件箱唤醒/系统通知迁移到 `useBackgroundTaskCoordinator`，账号创建/移除/连接验证/IMAP 映射迁移到 `useAccountConnectionController`，批量与会话级已读/星标/归档/移动/标签/删除编排迁移到 `useMessageCollectionActions`，邮件与会话加载迁移到 `useMailboxData`，连接诊断模型、验收编排和面板也已独立拆分；`App.tsx` 继续缩减为页面编排职责 |
| 多账号/统一邮箱 | 部分实现 | 支持创建多个账号、账号范围切换、唯一默认发件账号、统一邮箱虚拟系统文件夹、单账号/统一视图统计和列表隔离；新增账号会创建 7 个默认系统文件夹和默认发件身份并自动切换，统一邮箱写信优先使用默认账号，默认账号被移除后由剩余账号自动接管；账号移除要求输入完整邮箱地址、禁止删除最后一个账号、事务级联清理本地关联数据，并自动切换到剩余账号；账号范围已由原生下拉替换为双行弹出菜单，展示名称、选中态、服务商和完整邮箱摘要；已接入统一邮箱多账号聚合同步、IMAP 同步优先级选择、统一邮箱批次同步限流/下一批可视化与 SQLite 持久化任务队列，每个账号顺序同步六类核心目录和已绑定自定义目录并保留目录级失败警告；设置页可为自定义远端目录选择现有本地自定义文件夹、一键新建同名文件夹或解除映射，未映射时安全跳过；服务器测试、登录验证、IMAP 文件夹发现和同步演练始终以当前选中账号执行；多账号提醒策略待增强 |
| 邮件列表 | 已实现 | 首屏轻量分页、加载更多、分页上限、头像与悬停复选框、图标式高级搜索条件菜单、清空搜索/筛选、保存搜索快捷方式、未读/星标/附件过滤，并支持最新优先、最早优先、发件人/参与者和主题排序；排序偏好持久化并经过前后端白名单校验，非法值安全回退到最新优先。支持 `from:`、`to:`、`cc:`、`bcc:`、`subject:`、`label:`、`account:`、`mailbox:`、`folder:`、`filename:`、`attachment:`、`after:`、`before:`、`has:attachment`、`is:unread/read/starred` 搜索操作符 |
| 阅读与操作 | 已实现 | 已读/未读、文件夹右键全部标为已读、废纸篓右键清空、邮件右键“复制信息”子菜单复制发件人邮箱/邮件主题、星标、稍后处理/取消稍后、到期稍后邮件自动恢复收件箱、归档、删除、自定义文件夹创建/重命名/删除、菜单移动或拖入系统/自定义文件夹、手动标为垃圾邮件/不是垃圾邮件、废纸篓恢复/永久删除/清空、导出 `.eml`、标签切换、线程阅读、列表批量选择、批量已读/未读、批量星标、批量归档/删除/移动和批量打标签；线程列表右键可对整个会话切换已读/星标/静音、归档、移动、标签和删除，线程阅读页默认选中最新邮件，并常驻会话级星标、回复/回复全部、转发、归档、已读和删除，低频移动、标签与静音保留在更多菜单。会话静音按账号与线程键持久化并在列表显示紧凑标识，同一线程键不会跨账号互相影响；会话移动跳过草稿和已发送副本，多账号会话自动禁用跨账号移动；单封星标会回写远端 `\\Flagged`，统一邮箱虚拟文件夹按角色覆盖所有账号，真实文件夹仅更新自身，远端 UID 按账号和邮箱分组批量同步；移动到系统或已映射自定义文件夹时解析目标 mailbox，`COPYUID` 或 Message-ID 用于重绑定 UID，恢复到收件箱、永久删除与清空废纸篓会回写远端，批量清空按账号/目录执行 `\Deleted + UID EXPUNGE`，无法唯一定位时安全跳过；解除映射后不再尝试远端移动；拖拽只传邮件 ID，支持已选邮件批量拖动、合法目标高亮、同账号校验和移动后撤销；`Cmd/Ctrl + A` 可选择当前列表，`S/M/E/Delete` 优先处理已选邮件，`Esc` 在右键菜单关闭后可取消选择；阅读和批量操作采用主动作常驻 + 更多菜单折叠，标签采用当前标签 chip + 标签菜单，并支持归档/删除/移动/已读/星标/标签/垃圾邮件/稍后处理后的撤销 snackbar 与非编辑区域全局 `Cmd/Ctrl + Z`，七秒撤销状态由 `useUndoQueue` 管理，避免所有操作同时外露并降低误操作成本 |
| 稍后处理 | 已实现 | `snoozed_until` 持久化、稍后处理虚拟文件夹、阅读面稍后处理/取消稍后、恢复收件箱和刷新/同步时自动释放到期邮件已接入；统一选择器提供今天晚些时候、明天上午、本周末、下周一和自定义日期时间，并复用于邮件右键、线程右键、阅读页更多菜单、命令面板和批量操作栏 |
| 草稿 | 已实现 | 支持联系人自动补全、本地写信模板保存/插入/删除、写作过程 localStorage 自动保存和刷新后恢复、系统文件选择器与拖拽写信附件元数据/本地路径、保存草稿到草稿箱，并可从草稿箱继续编辑；显式保存采用本地优先策略，使用稳定 Message-ID 重建 MIME，在已映射 Drafts 中删除旧版本并以 `\Draft` APPEND 最新版本，成功后绑定远端 mailbox/UID；再次保存原地更新本地草稿和附件，远端失败不丢本地内容并显示具体原因 |
| 发送流转 | 部分实现 | 支持新邮件、默认极简写信界面、折叠式发件账号/身份/抄送/密送/模板/富文本/签名/稍后/附件工具、写信自动保存恢复、阅读面板内联快速回复、富文本 HTML 正文、发件账号选择、发件身份/别名选择、Reply-To、身份级签名插入、联系人自动补全、回复、回复全部、转发、系统文件选择器写信附件添加/移除、保存草稿；转发会自动携带已完整下载且本地路径有效的原附件，尚未下载的项目明确提示数量，HTML 原正文引用会回退到安全纯文本摘要。设置页可生成默认发给当前账号、主题带唯一编号的写入验收草稿，草稿无附件、无定时发送且不会自动提交，附件只能由用户手动添加，真实发送仍需在撰写器中再次确认。最近验证编号按账号持久化，状态面板结合发件箱记录和跨 outbox/Sent/Inbox 查询展示 SMTP、Sent 留档、自发自收、附件与远端可回写状态，并可定位对应邮件继续人工验证。回复、回复全部和快速回复会继承并去重原邮件引用链，草稿、撤销发送、稍后发送和 SMTP MIME 持久化标准 `In-Reply-To` / `References`。发送按钮支持关闭或 5/10/20/30 秒撤销延迟（默认 10 秒），倒计时内可持久化撤回到草稿箱，到期后自动触发 SMTP 后台任务。发件箱支持用户指定稍后发送和失败重试，并会按每封邮件所属账号取对应凭据触发真实 SMTP 发送，支持密码/授权码和 XOAUTH2 认证，纯文本/HTML 正文构建为 `multipart/alternative`，附件外层构建为 `multipart/mixed`；SMTP 成功后邮件立即进入本地已发送并关闭撤回，使用同一份带稳定 Message-ID 的原始 MIME 通过 IMAP `APPEND` 留档到远端 Sent，失败则显示“已发送 · 留档待重试”并只重试 APPEND，不会重复 SMTP；网易 163 已验证 SMTP 授权码认证，但未执行真实发送 |
| 附件 | 部分实现 | 已有附件元数据、UI 展示、写信附件 chip、写信拖拽投放区、草稿/发件箱/已发送附件元数据和本地路径保存、SMTP multipart 附件构建、按需下载、顺序“下载全部”、25 MB 解码后安全上限、100 MB 编码临时上限、IMAP BODYSTRUCTURE 路径与传输编码识别、BODY.PEEK part/chunk 分段写入，以及 BODYSTRUCTURE、整封回退和分段请求最多 3 次自动重试；前两次失败后按 150 ms、300 ms 指数退避。批量下载逐项执行以控制内存，单项失败继续后续项目；失败文件不会落为已下载状态，安全范围内的 `.download` 临时文件跨点击和应用重启保留，下次以文件长度作为 IMAP partial offset 续传；完成后流式解码 Base64、Quoted-Printable 或直通内容。阅读面显示附件总数和总大小、已保留进度、失败原因、重试按钮和下载中状态，成功后原子重命名并记录解码后大小，支持系统默认应用打开、另存为和转发复用；真实服务商 partial offset 与退避参数仍待采集 |
| 账号设置 | 部分实现 | 设置中心按账号、服务商、认证、发送、通知、隐私、身份、备份、同步、联系人、规则和安全预览拆为独立分页；本地保存 IMAP/SMTP 主机、认证方式、同步策略、签名、发件身份/别名、Reply-To、身份级签名、账号级远程图片策略和发件人/域名远程图片信任列表，支持新增账号和带完整邮箱确认的安全移除，并提供 Gmail/Outlook/QQ/网易预设、当前账号服务器测试、读取 Keychain 的 IMAP/SMTP 登录验证、文件夹发现、同步演练和脱敏诊断 JSON 导出；服务器测试与保存设置固定在标题栏，登录验证直接位于认证页且不会发送邮件；四步连接诊断区分成功、部分成功、凭据拒绝和网络异常，按服务商给出恢复建议并默认折叠技术详情；一键只读验收串联连接、登录、文件夹发现和首轮邮件头同步，SMTP 单独失败仍继续验证 IMAP，且全程不发送邮件或修改远端状态；发送与回写验收位于同步页首屏，只负责准备自发自收草稿并明确提示不得写入密码、授权码或 Token，生成后显示五阶段状态、刷新入口、已发送/收件定位按钮和已读/星标/归档/恢复四步回写引导 |
| OAuth2 | 部分实现 | Gmail/Outlook PKCE 授权 URL 生成、scope/state/code challenge、系统浏览器打开、授权会话持久化、本地回调监听、回调授权码记录、授权码 token 交换入 Keychain、refresh token 自动刷新和 IMAP/SMTP XOAUTH2 登录已实现；真实账号兼容性验证待补 |
| 服务商兼容性 | 部分实现 | 设置页展示 Gmail/Outlook/QQ/网易的 IMAP/SMTP、推荐认证、配置说明和验证限制，支持本地记录 IMAP/SMTP/OAuth/诊断导出验证结果；网易 163 已记录历史 TLS/授权码认证通过样本和后续凭据拒绝样本，当前凭据仍需重新验证；Gmail/Outlook/QQ 和真实发送/附件仍待验证 |
| 搜索 | 部分实现 | 支持当前文件夹、当前账号全部文件夹、全部账号全部文件夹三档范围，邮件与线程请求共享范围，保存搜索持久化并恢复范围；ASCII 查询走 SQLite FTS5，中文等非 ASCII 查询保留 LIKE 兼容策略 |
| 协议边界 | 部分实现 | TCP 服务器测试、IMAP 密码/XOAUTH2 登录验证、SMTP TLS/STARTTLS 认证与 `NOOP` 验证、IMAP 文件夹发现、远端文件夹映射、UIDVALIDITY 与最高/最低 UID 双游标、本地自定义文件夹引用、新邮件增量和历史邮件回填分离、首次最近 25 封、后续每目录每轮最多 25 封历史邮件、无跨页跳 UID、同一会话最近 50 封 `UID FLAGS` 对账、远端已读/星标刷新与近期服务器删除清理、目录级失败隔离、未映射目录保护、SQLite 持久化后台任务队列、按同步策略触发的定时同步、正文按需拉取、附件下载、单封远端状态回写、按账号/邮箱分组的批量 `UID STORE` 已读回写、单封 `\\Flagged` 星标回写、`UID MOVE` + `COPYUID`/Message-ID UID 重绑定、草稿旧版本 `\Deleted + UID EXPUNGE` 与 `\Draft` APPEND 替换、SMTP 后 IMAP `APPEND` Sent 留档、Message-ID 留档查重、`APPENDUID` 解析和无 UIDPLUS 时的搜索/UID 0 延迟重绑定、按账号/目录批量 `\Deleted + UID EXPUNGE`、MIME/HTML 安全预览和正式阅读面安全 HTML 渲染已实现 |
| 通知 | 部分实现 | Tauri notification 插件、桌面能力声明、权限检查/申请、新邮件摘要通知、免打扰时段、VIP 发件人、仅 VIP 提醒策略、账号静音、会话静音、重点账号提醒和系统未读角标已接入；静音按账号与线程键隔离，过滤后摘要数量只统计实际提醒邮件；Windows overlay icon 兼容待补 |
| 发件箱 | 部分实现 | 本地队列、稍后发送、0/5/10/20/30 秒撤销发送、倒计时 snackbar、撤回到草稿箱、到期自动 SMTP 调度、发送演练、附件传递到 SMTP MIME 构建、SMTP 失败重试、`sent_remote_pending` 远端留档重试状态和下次尝试时间已实现；SMTP 成功后立即移入本地已发送且不可撤回，留档失败五分钟后只重试 IMAP APPEND；最早到期项使用单个前端定时器，同类 queued/running 后台任务在 SQLite 层统一去重 |
| 联系人与规则 | 部分实现 | 联系人自动提取、联系人中心、联系人搜索、一键给联系人写信、加入当前草稿、侧栏联系人右键直达编辑表单/删除、命令面板联系人写信、联系人新建/编辑/删除、别名/VIP 后端持久化、手动合并、重复联系人建议合并、VIP 状态同步通知策略、本地 vCard 3.0/4.0 导入与 vCard 4.0 导出、按主邮箱去重并合并别名/VIP、IMAP 导入后自动执行规则打标/状态动作，支持可视化规则构建器、多动作规则、停止后续规则、规则新增/编辑/启停/删除，线程摘要和线程阅读视图已实现；CardDAV 与服务商联系人同步仍待补 |
| 安全 | 部分实现 | 系统 Keychain 已接入；原始邮件预览和正式阅读面均使用 `ammonia` 做 HTML sanitizer，并展示清洗、远程图片、外链、域名不一致、IP 跳转和登录/验证路径风险提示；远程图片默认阻止，支持按发件人或域名加入持久化信任列表后安全重渲染，并可阻止发件人自动移入垃圾邮件 |
| 性能基准 | 部分实现 | `npm run bench` 已输出前端总产物、首屏静态依赖、按需异步资源、Tauri 二进制/bundle 产物探测和当前进程内存采样，并明确缺口；2026-07-11 当前前端 dist 为 686.1 KB，其中首屏直接加载 472.1 KB、按需资源 213.6 KB，入口脚本为 350.4 KB，基准 Node 进程 RSS 为 58.19 MB。相比懒加载前约 619.8 KB 的主 JS + CSS，首屏资源下降约 24%；`npm run bench:sync` 已可采样 release App 前端 ready 时间、同步演练完成标记、采样窗口 peak RSS 和窗口末尾 RSS，历史 release 样本约 769.76ms ready / 83.72 MB peak RSS |

## 关键测试点

- 初始化数据库会创建核心文件夹、标签、种子邮件和附件元数据。
- 搜索、保存搜索快捷方式、过滤、附件查询和 `from:`/`to:`/`cc:`/`bcc:`/`subject:`/`label:`/`account:`/`mailbox:`/`folder:`/`filename:`/`attachment:`/`after:`/`before:`/`has:`/`is:` 操作符可组合使用。
- 邮件列表与会话摘要使用相同的账号、文件夹、搜索、筛选和分页作用域；空账号或无文件夹状态不会残留上一视图的线程结果。
- 邮件状态、移动文件夹、稍后处理时间、到期稍后邮件释放、标签增删可持久化。
- 草稿和本地发送会进入对应系统文件夹并保存写信附件元数据；草稿可重建带稳定 Message-ID 的 MIME，并持久化远端 Drafts mailbox/UID。
- 账号设置可编辑并持久化；新增账号会校验邮箱唯一性、创建默认系统文件夹和默认发件身份，并可立即切换到新账号或统一邮箱；默认发件账号在数据库中保持唯一，统一邮箱写信优先使用该账号，删除默认账号后剩余账号自动接管；账号移除要求完整邮箱确认并至少保留一个账号。
- 服务器测试、登录验证、IMAP 文件夹发现和同步演练均接收账号作用域；登录验证从 Keychain 读取密码/授权码或 OAuth2 token，分别执行 IMAP 登录和 SMTP 认证 + `NOOP`，不发送邮件；账号删除级联、凭据报告状态、同步演练、统一邮箱批次同步调度、核心目录筛选、原始邮件预览、发件箱队列/撤回/定时项/重试窗口、同类后台任务去重、联系人新建/删除/手动合并/重复联系人建议、联系人名称/别名/VIP 持久化编辑、自动规则执行/多动作规则/停止后续规则/规则编辑、线程摘要和线程消息查询均有后端测试覆盖。
- SMTP 端点解析、收件人拆分、稳定 Message-ID、IMAP 端点解析、远端文件夹角色映射、首次最近 UID 页、连续增量 UID 页、紧邻低水位历史 UID 页、历史游标持久化、UIDVALIDITY 重置、邮件头导入去重、无日志批量导入、自定义目录拒绝误归入收件箱、自定义目录绑定/解除绑定/目标文件夹导入、稍后处理持久化/恢复/到期释放、正文解析、附件元数据缓存、写信附件元数据落库、富文本 HTML 出站清洗落库、发件箱待发送 HTML 正文传递、发件箱待发送附件传递、SMTP 失败重试窗口、SMTP 成功后远端留档待重试、五分钟 APPEND 重试窗口、APPENDUID 解析、Sent mailbox/UID 最终绑定、SMTP multipart alternative/附件构建、附件下载路径落库、附件下载大小保护、BODYSTRUCTURE part/编码识别、150/300 ms 重试退避、Base64/Quoted-Printable 流式解码与解码后大小保护、文件名清理、MIME 解析安全提示、HTML sanitizer、正式阅读面安全 HTML 持久化、远程图片信任策略、Keychain 状态对象均有测试覆盖。
- OAuth2 Gmail 授权 URL、PKCE code challenge、scope、不支持服务商拒绝逻辑、授权会话持久化恢复、回调 state 校验、重复回调拒绝、token 请求构造、refresh token 请求构造、token 响应解析、IMAP XOAUTH2 响应和 SMTP XOAUTH2 机制均有 Rust 单元测试覆盖。
- 前端回复/转发、后台同步摘要、新邮件通知文案、免打扰/VIP/账号级通知策略、服务商矩阵一致性和远程图片信任输入已有 Vitest 单元测试；品牌标识、现代双行账号范围菜单与独立账号切换、右键设置默认发件账号/默认标识/统一邮箱默认发件、QQ 预设新增账号/自动切换/默认目录与身份、账号移除邮箱确认/回到已配置默认账号/菜单清理、侧栏折叠边界、常用邮箱固定与刷新持久化、文件夹右键全部已读/清空废纸篓、主界面、命令面板打开与执行、快捷键帮助按钮与键盘打开/关闭、搜索、保存搜索快捷方式、联系人中心搜索/写信、联系人右键直达编辑表单、命令面板联系人写信、联系人自动补全、联系人新建/编辑/建议合并/手动合并/删除、联系人名称/别名/VIP 编辑和 VIP 通知策略同步、写信高级工具默认折叠、写信自动保存恢复、写信模板保存/插入、写信附件按钮添加和拖拽添加、邮件拖入侧栏文件夹/目标高亮/移动后撤销、富文本 HTML 撰写、草稿保存、设置顶部服务器测试/保存主操作、登录验证 IMAP/SMTP 分项状态、服务商验证状态自动回填、OAuth2 PKCE 启动/回调/token 交换/token 刷新、第二账号服务器测试/登录验证/IMAP 文件夹发现/同步演练、手动同步扫描多个核心文件夹、撤销发送设置持久化、倒计时内撤回到草稿箱、到期自动进入已发送、内联快速回复发送、线程阅读、线程默认选中最新邮件、会话级星标与右键已读操作、列表批量星标/打标签、`Cmd/Ctrl + A` 全选、键盘批量动作与 `Esc` 取消选择、发件箱排队/撤回、设置、账号级通知入口、规则新增、原始 MIME 安全预览、稍后处理/取消稍后、归档后通过全局 `Cmd/Ctrl + Z` 恢复、单封标签切换、附件下载失败后重试和远程图片信任已有 Chrome headless UI 回归覆盖。

## 当前缺口

- OAuth2 已有认证模式、PKCE 授权页启动、授权会话恢复记录、本地回调监听、回调授权码记录、授权码 token 交换、token 存储入口、自动刷新和 IMAP/SMTP XOAUTH2 登录，后台同步已有轻量定时触发；设置页已加入真实账号兼容性采集表单，仍需填入实际账号验证样本。
- 多账号已有创建、安全移除、切换、统一虚拟文件夹、统计隔离、统一邮箱多账号聚合同步、IMAP 同步优先级选择、批次同步限流/下一批可视化、SQLite 持久化任务队列、账号级服务器测试/登录验证/IMAP 文件夹发现/同步演练、账号级通知静音/重点提醒、脱敏账号级诊断/本地备份导出；仍需补真实服务商多账号提醒样本。
- IMAP 已有真实登录文件夹发现入口、UI 兼容性矩阵、本地验证记录表单和远端草稿替换同步；网易 163 已通过基础登录认证，仍需验证文件夹发现、草稿 Drafts APPEND、正文/附件同步和状态回写，并补 Gmail/Outlook/QQ/自建邮箱结果。
- SMTP 已有真实发送入口、附件 multipart 构建、稳定 Message-ID、成功后 IMAP Sent 留档和独立留档重试状态机、UI 兼容性矩阵及本地验证记录表单；网易 163 已通过基础授权码认证，仍需真实发送/附件/远端 Sent 留档验证，并补 Gmail/Outlook/QQ/自建邮箱结果。
- 大附件已有下载前元数据保护、IMAP BODYSTRUCTURE part/传输编码识别、BODY.PEEK 分段写入、单次请求最多 3 次自动重试、150/300 ms 指数退避、Base64/Quoted-Printable 流式解码、解码后 25 MB 限制、失败后手动重试和跨应用重启 `.download` 断点续传；当前退避参数、partial offset 和异常服务商编码仍需真实账号样本校准。通知已有免打扰/VIP/账号级策略、应用未读角标和 SQLite 持久化后台任务队列，更成熟的真实账号多账号样本仍需增强。
- HTML sanitizer 已用于原始邮件安全预览和正式阅读面，并已有钓鱼域名比对、IP 跳转、登录/验证路径、远程图片风险提示和发件人/域名信任列表；仍需补真实 HTML 邮件样本兼容性。
- 已有网易 163 基础 TLS 与认证样本，但还没有真实远端同步/发送压力样本；UI 自动化已覆盖主要本地闭环，仍需补真实账号端到端回归。

## 下一阶段建议

1. 使用真实 Gmail/Outlook/QQ/自建邮箱账号填充兼容性验证记录，并扩展网易 163 的真实同步、发送与附件结果。
2. 完善大附件真实服务商兼容性矩阵和远端状态回写兼容性矩阵。
3. 做 SMTP 发送 + IMAP Sent 留档真实账号兼容性矩阵，覆盖 UIDPLUS/非 UIDPLUS、失败重试和服务商退避策略校准。
4. 补充真实 HTML 邮件样本兼容性验证和信任发件人图片策略。
5. 用真实账号同步样本扩展 `npm run bench:sync`，并补充真实 IMAP/SMTP 账号端到端 UI 回归。

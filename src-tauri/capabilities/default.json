{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default app permissions for Better Email desktop features.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:window:allow-set-badge-count",
    "core:window:allow-set-badge-label",
    "dialog:default",
    "notification:default"
  ]
}

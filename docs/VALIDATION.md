# Better Email 验证记录

## 当前验证时间

- 日期：2026-07-10
- 阶段：本地邮箱客户端原型；项目已统一命名为 `better-email` / `Better Email`，并加入旧版 localStorage、SQLite 数据库、Keychain 服务名和基准环境变量的兼容迁移；完成 QQ/Apple/Foxmail 参考下的平面三栏 UI 优化、统一右键菜单、文件夹全部标为已读/清空废纸篓、联系人右键编辑/删除、邮件拖入侧栏文件夹、常用邮箱固定与持久化、侧栏内约束的更多邮箱列表、安全移除邮箱账号、唯一默认发件账号、可配置撤销发送延迟、倒计时撤回与到期自动 SMTP 调度、IMAP 新邮件/历史邮件双向分页和设置页手动历史回填，并完成网易 163 的脱敏 TLS 与授权码认证验证；继续保留多账号、Keychain、MIME 安全预览、OAuth2/XOAUTH2、SMTP multipart、后台任务、通知、稍后处理、附件分段下载和远端状态回写入口

## 已验证命令

```bash
npm run build
npm run bench
npm run bench:release
npm run bench:app
npm run bench:sync
npm test
npm run test:ui
cargo fmt --manifest-path src-tauri/Cargo.toml
cargo test --manifest-path src-tauri/Cargo.toml
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
```

## 验证结果

- 前端生产构建通过：Vite/TypeScript 构建成功。
- 主界面视觉回归通过：在 1069×713 内容区确认三栏无横向裁切、40 封邮件正常加载、搜索条件菜单与阅读“更多”菜单不越界；在 1500×900 确认标题和操作栏并排、正文与快速回复完整利用宽屏空间。
- 侧栏视觉回归通过：品牌方块已由旧 `S` 修正为 `B`；常用邮箱增加一项后仍保持紧凑层级；展开“更多邮箱”时列表宽 244px、完整包含于 268px 侧栏内，不再被通用弹层规则拉伸到整窗宽度。
- 轻量基准入口通过：`npm run bench` 可输出前端构建产物、最大前端资源、Tauri debug/release 二进制、bundle 产物、样本 SQLite、当前进程内存采样和未覆盖缺口；`npm run bench:release` 使用 `tauri build --no-bundle` 生成 release 二进制后复测体积；`npm run bench:app` 可启动 release App、等待采样窗口并记录空闲 RSS 后退出。
- Release 体积样本：`npm run bench:release` 已生成 `/src-tauri/target/release/better-email`，当前二进制约 17.72 MB；真实挂载后的前端 dist 约 237.5 KB。Cargo release 目录是构建缓存，不作为用户可见 App 体积。
- Release App 启动/同步峰值/空闲内存样本：`BETTER_EMAIL_BENCH_SAMPLE_MS=8000 npm run bench:sync` 已成功启动 `/src-tauri/target/release/better-email`，采样窗口 8 秒，前端 ready 标记约 769.76ms，同步演练完成标记约 769.81ms，采样窗口 peak RSS 约 83.72 MB，窗口末尾 RSS 约 83.23 MB；此前破损 PNG 图标导致的 `invalid icon` 启动崩溃已通过重建标准 Tauri 多尺寸图标资源修复。
- 前端单元测试通过：10 个 Vitest 测试覆盖日期/附件大小格式化、回复/转发主题前缀、引用正文生成、同步状态摘要、新邮件通知文案、多账号通知路由、服务商矩阵/预设一致性和远程图片信任输入生成。
- 自动化 UI 回归通过：`npm run test:ui` 的 71 个核心断言使用 Chrome headless + mock Tauri 数据验证 Better Email 品牌标识、旧 `swiftmail.*` 设置键迁移、三栏布局持久化/重置、现代账号范围弹出菜单/独立账号切换、右键设置默认发件账号及默认标识、统一邮箱写信使用默认账号、QQ 预设新增账号、自动切换、默认系统文件夹和默认发件身份、账号移除完整邮箱确认、删除后回到已配置默认账号且账号菜单清理、更多邮箱列表边界、常用邮箱固定/刷新持久化/取消固定、文件夹右键全部标为已读/远端批量清空废纸篓、联系人右键编辑/删除入口、头像邮件列表、单封星标远端 `\\Flagged` 回写提示、邮件右键复制发件人邮箱/邮件主题、图标式高级搜索菜单、统一右键菜单及键盘子菜单、邮件拖入文件夹与撤销、全局 `Cmd/Ctrl + Z` 邮件操作撤销、`Cmd/Ctrl + A` 全选当前列表、键盘批量操作和 `Esc` 取消选择、直接删除、远端恢复到收件箱与永久 expunge、命令面板、线程阅读、保存搜索、联系人、写信自动保存/模板/附件/富文本/身份/签名、设置标题栏固定服务器测试/保存主操作、Keychain 凭据登录验证与 IMAP/SMTP 分项结果、服务商真实验证状态自动回填、OAuth2 PKCE 启动/回调授权码记录/token 交换/token 刷新、第二账号服务器测试/登录验证/IMAP 文件夹发现/同步演练作用域、远端自定义目录一键创建同名本地文件夹并映射、手动历史回填一页及最低 UID 状态更新、手动同步一次扫描多个已映射文件夹、已映射自定义目录作为真实远端移动目标、撤销发送设置持久化、倒计时内撤回到草稿箱、到期自动进入已发送、批量动作、自定义文件夹、垃圾邮件、发件箱、设置、本地 EML 导入、本地备份、规则、原始 MIME 安全预览、稍后处理、撤销、快速回复、EML 导出、标签、附件下载、阻止发件人和远程图片信任。
- Rust 单元测试通过：90 项测试覆盖旧 `swiftmail.sqlite3` 数据库及 WAL 边车文件迁移、凭据验证报告成功/部分失败状态、默认发件账号唯一性/切换/删除接管、账号删除约束和关联数据级联清理、本地 EML 导入落库、安全正文、联系人和附件文件、OAuth2、IMAP/SMTP XOAUTH2、真实与虚拟文件夹批量已读、远端 UID 集合去重、首次最近页/增量页/历史页 UID 选择、无游标或 UIDVALIDITY 重置时的历史同步引导、历史最低 UID 游标推进与完成状态、邮件头远端星标导入、最近 FLAGS 快照更新已读/星标并移除服务器已删除邮件、UIDVALIDITY 变化时清除旧 UID 数据、`COPYUID` 目标 UID 解析、IMAP 参数转义与换行拒绝、移动后 UID 0 记录按 Message-ID 原地重绑定、废纸篓删除前远端引用快照、核心目录和已绑定自定义目录筛选、自定义远端目录拒绝误导入收件箱、自定义目录绑定/目标文件夹导入/解除绑定保护、已映射自定义文件夹远端移动目标解析与解除映射保护、无日志批量邮件头导入、数据层、协议、SMTP、IMAP 探测/映射/同步、后台任务、本地备份、稍后处理、发件箱重试、远程图片信任、联系人、规则、附件下载/大小保护、富文本出站清洗、SMTP multipart、HTML sanitizer、发件箱撤回和凭据边界。
- 网易 163 真实账号基础验证通过：2026-07-10 对 `imap.163.com:993` 与 `smtp.163.com:465` 完成 TLS 1.3 握手、证书链校验和客户端授权码认证；未保存账号、授权码或完整凭据到仓库/文档/日志，且未执行真实邮件发送。本结果仅证明基础连接与认证，附件、正文同步、状态回写、真实发送和压力样本仍需单独验证。
- Clippy 通过：`cargo clippy --all-targets -- -D warnings` 无警告。

## 已覆盖功能

| 功能域 | 当前状态 | 说明 |
| --- | --- | --- |
| 跨平台桌面壳 | 已实现 | Tauri + 系统 WebView，避免 Electron 常驻 Chromium；release 启动图标资源已重建为 PNG/ICNS/ICO 多尺寸资产 |
| 本地数据库 | 已实现 | SQLite + WAL，含账号、文件夹、邮件、标签、附件元数据，并支持不含 Keychain 凭据的本地 JSON 备份/恢复 |
| 三栏邮箱 UI | 已实现 | 文件夹栏、头像邮件列表、线程列表、阅读面板、撰写窗口、设置窗口，并支持侧边栏/邮件列表拖拽宽度、localStorage 持久化和恢复默认；低频文件夹可固定到常用邮箱并在刷新后保留，更多邮箱始终限制在侧栏内滚动；采用平面分栏、中性选中态和紧凑图标栏，高频回复/转发/归档/已读/删除直接可见，高级搜索、低频整理和安全动作按需展开；设置窗口由 `SettingsFrame` 统一管理分类导航、纯图标无障碍关闭和顶部服务器测试/保存操作，600px 窄窗口下主操作自动收为 32px 图标；下拉框、复选框、右键菜单、紧凑菜单、弹窗和右下状态浮层已统一最终视觉层，并确保 `ui-2026.css` 在组件样式后加载；认证区四个操作支持自动换行，并直接显示登录总状态与 IMAP/SMTP 分项卡片；`Sidebar` 的账号切换和文件夹导航分别拆分到 `AccountSwitcher`、`SidebarFolderNavigation`，账号危险区、联系人管理和 OAuth2 流程也已拆为独立组件/Hook；命令条目与过滤迁移到 `useCommandPaletteItems`，全局快捷键迁移到 `useAppShortcuts`，`App.tsx` 由 3074 行缩减至 2810 行，仍继续迁移同步与设置业务编排 |
| 多账号/统一邮箱 | 部分实现 | 支持创建多个账号、账号范围切换、唯一默认发件账号、统一邮箱虚拟系统文件夹、单账号/统一视图统计和列表隔离；新增账号会创建 7 个默认系统文件夹和默认发件身份并自动切换，统一邮箱写信优先使用默认账号，默认账号被移除后由剩余账号自动接管；账号移除要求输入完整邮箱地址、禁止删除最后一个账号、事务级联清理本地关联数据，并自动切换到剩余账号；账号范围已由原生下拉替换为双行弹出菜单，展示名称、选中态、服务商和完整邮箱摘要；已接入统一邮箱多账号聚合同步、IMAP 同步优先级选择、统一邮箱批次同步限流/下一批可视化与 SQLite 持久化任务队列，每个账号顺序同步六类核心目录和已绑定自定义目录并保留目录级失败警告；设置页可为自定义远端目录选择现有本地自定义文件夹、一键新建同名文件夹或解除映射，未映射时安全跳过；服务器测试、登录验证、IMAP 文件夹发现和同步演练始终以当前选中账号执行；多账号提醒策略待增强 |
| 邮件列表 | 已实现 | 首屏轻量分页、加载更多、分页上限、头像与悬停复选框、图标式高级搜索条件菜单、清空搜索/筛选、保存搜索快捷方式、未读/星标/附件过滤，并支持 `from:`、`to:`、`cc:`、`bcc:`、`subject:`、`label:`、`account:`、`mailbox:`、`folder:`、`filename:`、`attachment:`、`after:`、`before:`、`has:attachment`、`is:unread/read/starred` 搜索操作符 |
| 阅读与操作 | 已实现 | 已读/未读、文件夹右键全部标为已读、废纸篓右键清空、邮件右键“复制信息”子菜单复制发件人邮箱/邮件主题、星标、稍后处理/取消稍后、到期稍后邮件自动恢复收件箱、归档、删除、自定义文件夹创建/重命名/删除、菜单移动或拖入系统/自定义文件夹、手动标为垃圾邮件/不是垃圾邮件、废纸篓恢复/永久删除/清空、导出 `.eml`、标签切换、线程阅读、列表批量选择、批量已读/未读、批量星标、批量归档/删除/移动和批量打标签；单封星标会回写远端 `\\Flagged`，统一邮箱虚拟文件夹按角色覆盖所有账号，真实文件夹仅更新自身，远端 UID 按账号和邮箱分组批量同步；移动到系统或已映射自定义文件夹时解析目标 mailbox，`COPYUID` 或 Message-ID 用于重绑定 UID，恢复到收件箱、永久删除与清空废纸篓会回写远端，批量清空按账号/目录执行 `\Deleted + UID EXPUNGE`，无法唯一定位时安全跳过；解除映射后不再尝试远端移动；拖拽只传邮件 ID，支持已选邮件批量拖动、合法目标高亮、同账号校验和移动后撤销；`Cmd/Ctrl + A` 可选择当前列表，`S/M/E/Delete` 优先处理已选邮件，`Esc` 在右键菜单关闭后可取消选择；阅读和批量操作采用主动作常驻 + 更多菜单折叠，标签采用当前标签 chip + 标签菜单，并支持归档/删除/移动/已读/星标/标签/垃圾邮件/稍后处理后的撤销 snackbar 与非编辑区域全局 `Cmd/Ctrl + Z`，七秒撤销状态由 `useUndoQueue` 管理，避免所有操作同时外露并降低误操作成本 |
| 稍后处理 | 已实现 | `snoozed_until` 持久化、稍后处理虚拟文件夹、阅读面稍后处理/取消稍后、恢复收件箱和刷新/同步时自动释放到期邮件已接入 |
| 草稿 | 已实现 | 支持联系人自动补全、本地写信模板保存/插入/删除、写作过程 localStorage 自动保存和刷新后恢复、系统文件选择器与拖拽写信附件元数据/本地路径、保存草稿到草稿箱，并可从草稿箱继续编辑；再次保存会原地更新草稿和附件而非复制新草稿 |
| 发送流转 | 部分实现 | 支持新邮件、默认极简写信界面、折叠式发件账号/身份/抄送/密送/模板/富文本/签名/稍后/附件工具、写信自动保存恢复、阅读面板内联快速回复、富文本 HTML 正文、发件账号选择、发件身份/别名选择、Reply-To、身份级签名插入、联系人自动补全、回复、回复全部、转发、系统文件选择器写信附件添加/移除、保存草稿；发送按钮支持关闭或 5/10/20/30 秒撤销延迟（默认 10 秒），倒计时内可持久化撤回到草稿箱，到期后自动触发 SMTP 后台任务。发件箱支持用户指定稍后发送和失败重试，并会按每封邮件所属账号取对应凭据触发真实 SMTP 发送，支持密码/授权码和 XOAUTH2 认证，纯文本/HTML 正文构建为 `multipart/alternative`，附件外层构建为 `multipart/mixed`；网易 163 已验证 SMTP 授权码认证，但未执行真实发送 |
| 附件 | 部分实现 | 已有附件元数据、UI 展示、写信附件 chip、写信拖拽投放区、草稿/发件箱/已发送附件元数据和本地路径保存、SMTP multipart 附件构建、按需下载、25 MB 安全上限、IMAP BODYSTRUCTURE 定位、BODY.PEEK part/chunk 分段写入、本地路径记录、系统默认应用打开和系统保存对话框另存为，真实服务商兼容性待采集 |
| 账号设置 | 部分实现 | 本地保存 IMAP/SMTP 主机、认证方式、同步策略、签名、发件身份/别名、Reply-To、身份级签名、账号级远程图片策略和发件人/域名远程图片信任列表，支持新增账号和带完整邮箱确认的安全移除，并提供 Gmail/Outlook/QQ/网易预设、当前账号服务器测试、读取 Keychain 的 IMAP/SMTP 登录验证、文件夹发现、同步演练和脱敏诊断 JSON 导出；服务器测试与保存设置固定在标题栏，登录验证直接位于认证区且不会发送邮件 |
| OAuth2 | 部分实现 | Gmail/Outlook PKCE 授权 URL 生成、scope/state/code challenge、系统浏览器打开、授权会话持久化、本地回调监听、回调授权码记录、授权码 token 交换入 Keychain、refresh token 自动刷新和 IMAP/SMTP XOAUTH2 登录已实现；真实账号兼容性验证待补 |
| 服务商兼容性 | 部分实现 | 设置页展示 Gmail/Outlook/QQ/网易的 IMAP/SMTP、推荐认证、配置说明和验证限制，支持本地记录 IMAP/SMTP/OAuth/诊断导出验证结果；网易 163 已完成 TLS 与授权码认证验证并在矩阵标为“真实账号已验证”，Gmail/Outlook/QQ 和真实发送/附件仍待验证 |
| 搜索 | 部分实现 | ASCII 查询走 SQLite FTS5，中文等非 ASCII 查询保留 LIKE 兼容策略 |
| 协议边界 | 部分实现 | TCP 服务器测试、IMAP 密码/XOAUTH2 登录验证、SMTP TLS/STARTTLS 认证与 `NOOP` 验证、IMAP 文件夹发现、远端文件夹映射、UIDVALIDITY 与最高/最低 UID 双游标、本地自定义文件夹引用、新邮件增量和历史邮件回填分离、首次最近 25 封、后续每目录每轮最多 25 封历史邮件、无跨页跳 UID、同一会话最近 50 封 `UID FLAGS` 对账、远端已读/星标刷新与近期服务器删除清理、目录级失败隔离、未映射目录保护、SQLite 持久化后台任务队列、按同步策略触发的定时同步、正文按需拉取、附件下载、单封远端状态回写、按账号/邮箱分组的批量 `UID STORE` 已读回写、单封 `\\Flagged` 星标回写、`UID MOVE` + `COPYUID`/Message-ID UID 重绑定、按账号/目录批量 `\Deleted + UID EXPUNGE`、MIME/HTML 安全预览和正式阅读面安全 HTML 渲染已实现 |
| 通知 | 部分实现 | Tauri notification 插件、桌面能力声明、权限检查/申请、新邮件摘要通知、免打扰时段、VIP 发件人、仅 VIP 提醒策略、账号静音、重点账号提醒和系统未读角标已接入；Windows overlay icon 兼容待补 |
| 发件箱 | 部分实现 | 本地队列、稍后发送、0/5/10/20/30 秒撤销发送、倒计时 snackbar、撤回到草稿箱、到期自动 SMTP 调度、发送演练、附件传递到 SMTP MIME 构建、失败重试状态和下次尝试时间已实现；最早到期项使用单个前端定时器，同类 queued/running 后台任务在 SQLite 层统一去重 |
| 联系人与规则 | 部分实现 | 联系人自动提取、联系人中心、联系人搜索、一键给联系人写信、加入当前草稿、侧栏联系人右键直达编辑表单/删除、命令面板联系人写信、联系人新建/编辑/删除、别名/VIP 后端持久化、手动合并、重复联系人建议合并、VIP 状态同步通知策略、IMAP 导入后自动执行规则打标/状态动作，支持可视化规则构建器、多动作规则、停止后续规则、规则新增/编辑/启停/删除，线程摘要和线程阅读视图已实现；真实账号联系人导入仍待补 |
| 安全 | 部分实现 | 系统 Keychain 已接入；原始邮件预览和正式阅读面均使用 `ammonia` 做 HTML sanitizer，并展示清洗、远程图片、外链、域名不一致、IP 跳转和登录/验证路径风险提示；远程图片默认阻止，支持按发件人或域名加入持久化信任列表后安全重渲染，并可阻止发件人自动移入垃圾邮件 |
| 性能基准 | 部分实现 | 已有 `npm run bench` 输出前端产物、Tauri 二进制/bundle 产物探测和当前进程内存采样，并明确缺口；`npm run bench:sync` 已可采样 release App 前端 ready 时间、同步演练完成标记、采样窗口 peak RSS 和窗口末尾 RSS，当前样本约 769.76ms ready / 83.72 MB peak RSS |

## 关键测试点

- 初始化数据库会创建核心文件夹、标签、种子邮件和附件元数据。
- 搜索、保存搜索快捷方式、过滤、附件查询和 `from:`/`to:`/`cc:`/`bcc:`/`subject:`/`label:`/`account:`/`mailbox:`/`folder:`/`filename:`/`attachment:`/`after:`/`before:`/`has:`/`is:` 操作符可组合使用。
- 邮件状态、移动文件夹、稍后处理时间、到期稍后邮件释放、标签增删可持久化。
- 草稿和本地发送会进入对应系统文件夹，并保存写信附件元数据。
- 账号设置可编辑并持久化；新增账号会校验邮箱唯一性、创建默认系统文件夹和默认发件身份，并可立即切换到新账号或统一邮箱；默认发件账号在数据库中保持唯一，统一邮箱写信优先使用该账号，删除默认账号后剩余账号自动接管；账号移除要求完整邮箱确认并至少保留一个账号。
- 服务器测试、登录验证、IMAP 文件夹发现和同步演练均接收账号作用域；登录验证从 Keychain 读取密码/授权码或 OAuth2 token，分别执行 IMAP 登录和 SMTP 认证 + `NOOP`，不发送邮件；账号删除级联、凭据报告状态、同步演练、统一邮箱批次同步调度、核心目录筛选、原始邮件预览、发件箱队列/撤回/定时项/重试窗口、同类后台任务去重、联系人新建/删除/手动合并/重复联系人建议、联系人名称/别名/VIP 持久化编辑、自动规则执行/多动作规则/停止后续规则/规则编辑、线程摘要和线程消息查询均有后端测试覆盖。
- SMTP 端点解析、收件人拆分、IMAP 端点解析、远端文件夹角色映射、首次最近 UID 页、连续增量 UID 页、紧邻低水位历史 UID 页、历史游标持久化、UIDVALIDITY 重置、邮件头导入去重、无日志批量导入、自定义目录拒绝误归入收件箱、自定义目录绑定/解除绑定/目标文件夹导入、稍后处理持久化/恢复/到期释放、正文解析、附件元数据缓存、写信附件元数据落库、富文本 HTML 出站清洗落库、发件箱待发送 HTML 正文传递、发件箱待发送附件传递、发件箱失败重试窗口、SMTP multipart alternative/附件构建、附件下载路径落库、附件下载大小保护、BODYSTRUCTURE part 定位、文件名清理、MIME 解析安全提示、HTML sanitizer、正式阅读面安全 HTML 持久化、远程图片信任策略、Keychain 状态对象均有测试覆盖。
- OAuth2 Gmail 授权 URL、PKCE code challenge、scope、不支持服务商拒绝逻辑、授权会话持久化恢复、回调 state 校验、重复回调拒绝、token 请求构造、refresh token 请求构造、token 响应解析、IMAP XOAUTH2 响应和 SMTP XOAUTH2 机制均有 Rust 单元测试覆盖。
- 前端回复/转发、后台同步摘要、新邮件通知文案、免打扰/VIP/账号级通知策略、服务商矩阵一致性和远程图片信任输入已有 Vitest 单元测试；品牌标识、现代双行账号范围菜单与独立账号切换、右键设置默认发件账号/默认标识/统一邮箱默认发件、QQ 预设新增账号/自动切换/默认目录与身份、账号移除邮箱确认/回到已配置默认账号/菜单清理、侧栏折叠边界、常用邮箱固定与刷新持久化、文件夹右键全部已读/清空废纸篓、主界面、命令面板打开与执行、快捷键帮助按钮与键盘打开/关闭、搜索、保存搜索快捷方式、联系人中心搜索/写信、联系人右键直达编辑表单、命令面板联系人写信、联系人自动补全、联系人新建/编辑/建议合并/手动合并/删除、联系人名称/别名/VIP 编辑和 VIP 通知策略同步、写信高级工具默认折叠、写信自动保存恢复、写信模板保存/插入、写信附件按钮添加和拖拽添加、邮件拖入侧栏文件夹/目标高亮/移动后撤销、富文本 HTML 撰写、草稿保存、设置顶部服务器测试/保存主操作、登录验证 IMAP/SMTP 分项状态、服务商验证状态自动回填、OAuth2 PKCE 启动/回调/token 交换/token 刷新、第二账号服务器测试/登录验证/IMAP 文件夹发现/同步演练、手动同步扫描多个核心文件夹、撤销发送设置持久化、倒计时内撤回到草稿箱、到期自动进入已发送、内联快速回复发送、线程阅读、列表批量星标/打标签、`Cmd/Ctrl + A` 全选、键盘批量动作与 `Esc` 取消选择、发件箱排队/撤回、设置、账号级通知入口、规则新增、原始 MIME 安全预览、稍后处理/取消稍后、归档后通过全局 `Cmd/Ctrl + Z` 恢复、单封标签切换、附件下载和远程图片信任已有 Chrome headless UI 回归覆盖。

## 当前缺口

- OAuth2 已有认证模式、PKCE 授权页启动、授权会话恢复记录、本地回调监听、回调授权码记录、授权码 token 交换、token 存储入口、自动刷新和 IMAP/SMTP XOAUTH2 登录，后台同步已有轻量定时触发；设置页已加入真实账号兼容性采集表单，仍需填入实际账号验证样本。
- 多账号已有创建、安全移除、切换、统一虚拟文件夹、统计隔离、统一邮箱多账号聚合同步、IMAP 同步优先级选择、批次同步限流/下一批可视化、SQLite 持久化任务队列、账号级服务器测试/登录验证/IMAP 文件夹发现/同步演练、账号级通知静音/重点提醒、脱敏账号级诊断/本地备份导出；仍需补真实服务商多账号提醒样本。
- IMAP 已有真实登录文件夹发现入口、UI 兼容性矩阵和本地验证记录表单；网易 163 已通过基础登录认证，仍需验证文件夹发现、正文/附件同步和状态回写，并补 Gmail/Outlook/QQ/自建邮箱结果。
- SMTP 已有真实发送入口、附件 multipart 构建、UI 兼容性矩阵和本地验证记录表单；网易 163 已通过基础授权码认证，仍需真实发送/附件验证，并补 Gmail/Outlook/QQ/自建邮箱结果。
- 大附件已有下载前安全上限、IMAP BODYSTRUCTURE part 定位和 BODY.PEEK 分段写入；通知已有免打扰/VIP/账号级策略、应用未读角标和 SQLite 持久化后台任务队列，更成熟的真实账号多账号样本仍需增强。
- HTML sanitizer 已用于原始邮件安全预览和正式阅读面，并已有钓鱼域名比对、IP 跳转、登录/验证路径、远程图片风险提示和发件人/域名信任列表；仍需补真实 HTML 邮件样本兼容性。
- 已有网易 163 基础 TLS 与认证样本，但还没有真实远端同步/发送压力样本；UI 自动化已覆盖主要本地闭环，仍需补真实账号端到端回归。

## 下一阶段建议

1. 使用真实 Gmail/Outlook/QQ/自建邮箱账号填充兼容性验证记录，并扩展网易 163 的真实同步、发送与附件结果。
2. 完善大附件真实服务商兼容性矩阵和远端状态回写兼容性矩阵。
3. 做 SMTP 真实账号兼容性矩阵、失败重试实测和服务商退避策略校准。
4. 补充真实 HTML 邮件样本兼容性验证和信任发件人图片策略。
5. 用真实账号同步样本扩展 `npm run bench:sync`，并补充真实 IMAP/SMTP 账号端到端 UI 回归。
